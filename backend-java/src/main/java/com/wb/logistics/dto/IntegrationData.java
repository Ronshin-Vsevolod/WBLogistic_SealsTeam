package com.wb.logistics.dto;

import java.util.List;

/**
 * Данные от внешних интеграций, передаваемые в Python ML.
 * Java собирает их из заглушек (или реальных API в продакшне),
 * Python ML использует как фичи моделей.
 *
 * Java НЕ интерпретирует эти данные — просто передаёт.
 */
public record IntegrationData(

        // ── Для MICRO модели ──

        /**
         * Погода на ближайшие 12 часов.
         * Каждый элемент — severity от 0.0 (ясно) до 10.0 (шторм).
         * Длина списка = 5 (по одному значению на 3 часа с сейчас).
         */
        List<Double> microWeather,

        /**
         * Коэффициент пробок для micro.
         * 1.0 = свободно, 10.0 = коллапс.
         */
        double traffic,

        // ── Для MACRO модели ──

        /**
         * Средняя погода по дням на 7 дней.
         * Каждый элемент — severity 0.0–10.0.
         * Длина списка = 7.
         */
        List<Double> macroWeather,

        /**
         * Промо-коэффициенты недели по дням для macro.
         * 1.0 = нет акций, >1.0 = промо (больше заказов).
         */
        List<Double> promo
) {
}